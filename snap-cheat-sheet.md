# snap
useful commands.

## installed packages
```
snap list
```

## update packages
if package not specified, all packages are refreshed.

```
sudo snap refresh [package]
```

just check for updates:
```
snap refresh --list
```

